# Copy this file to secrets.yaml and replace every placeholder.
# Never commit secrets.yaml or paste its contents into an issue.
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
fallback_ap_password: "CHANGE_THIS_FALLBACK_PASSWORD"
# Generate with: openssl rand -base64 32
api_encryption_key: "BASE64_32_BYTE_KEY"
ota_password: "CHANGE_THIS_OTA_PASSWORD"
